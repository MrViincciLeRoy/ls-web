# LSuite Environment Variables
# Copy this file to .env and fill in your values

# Flask Configuration
FLASK_ENV=development
FLASK_APP=app.py
SECRET_KEY=your-secret-key-here-change-in-production

# ============================================================================
# DATABASE CONFIGURATION - OFFLINE/ONLINE MODE
# ============================================================================

# OFFLINE_MODE: Set to 'true' to use SQLite (no PostgreSQL needed)
# Set to 'false' or remove to use PostgreSQL (requires PostgreSQL server)
OFFLINE_MODE=false

# Database URL (only used when OFFLINE_MODE=false)
# For PostgreSQL (ONLINE MODE):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lsuite

# Note: SQLite database will be automatically created at: data/lsuite.db
# when OFFLINE_MODE=true

# ============================================================================
# QUICK START OPTIONS
# ============================================================================

# Option 1: OFFLINE MODE (No PostgreSQL needed)
# - Set: OFFLINE_MODE=true
# - Run: python app.py
# - Uses SQLite database at data/lsuite.db
# - Perfect for development/testing

# Option 2: ONLINE MODE (Requires PostgreSQL)
# - Set: OFFLINE_MODE=false (or remove the line)
# - Ensure PostgreSQL is running
# - Run: python app.py
# - Uses PostgreSQL database
# - Better for production

# Option 3: Use Batch Files (Windows)
# - RUN_OFFLINE.bat - Automatically uses SQLite
# - RUN_ONLINE.bat - Uses PostgreSQL (checks if running)
# - START_LIQUIDSUITE.bat - Auto-detects best option

# ============================================================================

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:5000/gmail/oauth/callback

# ERPNext (optional - can be configured per instance in UI)
ERPNEXT_BASE_URL=https://your-erpnext-instance.com
ERPNEXT_API_KEY=your-api-key
ERPNEXT_API_SECRET=your-api-secret

# Celery (for background tasks - optional)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Logging
LOG_LEVEL=INFO
LOG_FILE=lsuite.log
